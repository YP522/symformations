{% extends 'base.html.twig' %}

{% block title %}Edit Organisme{% endblock %}

{% block body %}
    <h1>Modifier : Organisme</h1>

    {{ include('organisme/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_organisme_index') }}" class="button button-outline">retour à la liste</a>

    {{ include('organisme/_delete_form.html.twig') }}
{% endblock %}
