{% extends 'base.html.twig' %}

{% block title %}Organisme{% endblock %}

{% block body %}
    <h1>Organisme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ organisme.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ organisme.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ organisme.adresse }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ organisme.tel }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ organisme.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_organisme_index') }}">retour aux enregistrements</a>

    <a href="{{ path('app_organisme_edit', {'id': organisme.id}) }}">modifier</a>

    {{ include('organisme/_delete_form.html.twig') }}
{% endblock %}
