{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <h1>Salle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ salle.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salle_index') }}" class="button button-outline">retour à la liste</a>

    <a href="{{ path('app_salle_edit', {'id': salle.id}) }}" class="button"><i class="fa-solid fa-pen-to-square"></i>modifier</a>

    {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
