{% extends 'base.html.twig' %}

{% block title %}Promotion{% endblock %}

{% block body %}
    <h1>Promotion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promotion.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ promotion.nom }}</td>
            </tr>
            <tr>
                <th>Formation</th>
                <td>{{ promotion.formation }}</td>
            </tr>
            <tr>
                <th>Referent</th>
                <td>{{ promotion.referent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_promotion_index') }}">retour aux enregistrements</a>

    <a href="{{ path('app_promotion_edit', {'id': promotion.id}) }}">modifier</a>

    {{ include('promotion/_delete_form.html.twig') }}
{% endblock %}
