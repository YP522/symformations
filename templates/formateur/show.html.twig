{% extends 'base.html.twig' %}

{% block title %}Formateur{% endblock %}

{% block body %}
    <h1>Formateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formateur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ formateur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ formateur.prenom }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ formateur.tel }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ formateur.email }}</td>
            </tr>
            <tr>
                <th>Promotion</th>
                <td>{{ formateur.promotion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formateur_index') }}">retour aux enregistrements</a>

    <a href="{{ path('app_formateur_edit', {'id': formateur.id}) }}">modifier</a>

    {{ include('formateur/_delete_form.html.twig') }}
{% endblock %}
