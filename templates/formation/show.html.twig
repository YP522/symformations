{% extends 'base.html.twig' %}

{% block title %}Formation{% endblock %}

{% block body %}
    <h1>Formation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ formation.intitule }}</td>
            </tr>
            <tr>
                <th>Organisme</th>
                <td>{{ formation.organisme }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formation_index') }}">retour aux enregistrements</a>

    <a href="{{ path('app_formation_edit', {'id': formation.id}) }}">modifier</a>

    {{ include('formation/_delete_form.html.twig') }}
{% endblock %}
